# Copie pra .env em dev, ou configure as vars no painel do EasyPanel em prod.

# URL pública do backend (ex: Cloudflare Tunnel no PC local 24/7).
# Em DEV, deixe vazio — o proxy do Vite redireciona pro Flask local.
# Em PROD, aponte pra URL HTTPS do tunnel ou do domínio do backend.
VITE_API_URL=
